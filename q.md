Here's what you need to build in JS:
Step 1 — Make a quotes array of objects with text and author (add 8–10 quotes)
Step 2 — Track a currentIndex variable (start at 0)
Step 3 — New Quote button → pick a random quote using Math.random(), update #quote-text, #quote-author, and #counter
Step 4 — Copy button → use navigator.clipboard.writeText(...) to copy the quote, then show the "Copied!" message briefly
All the HTML IDs you need: quote-text, quote-author, counter, new-btn, copy-btn, copied-msg